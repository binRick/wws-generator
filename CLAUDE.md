# CLAUDE.md — wws-generator

Goal of this repo: **read and generate WeCreat MakeIt! `.wws` files** for a WeCreat
60W laser (WeCreat Vision), so designs can be produced/converted programmatically
instead of only by hand in the closed MakeIt! GUI.

## The one thing to know

`.wws` is **plain JSON** whose object model is **Fabric.js**. WeCreat markets it as a
closed, no-export format, but the file is fully readable *and writable*. **We have
confirmed a hand-built `.wws` opens in MakeIt!** as a correct 100×100 mm cut square
(`samples/square-100.known-good.wws`, produced by `src/generate-square.js`).

## Read this first

- **`docs/wws-format.md`** — full reverse-engineered spec: top-level structure,
  Fabric object schema, `path` command arrays, `processList` (laser power/speed),
  `layerDataList`, units/scaling math, color conventions, version differences.
- **`samples/`**:
  - `square-test.original.wws` — user's hand-made reference (v3.0.4), used as the
    generation template.
  - `square-100.known-good.wws` — generated by us, **confirmed to open in MakeIt!**.
  - `square-test.annotated.json` — pretty-printed (cover truncated) for easy reading.
- **`src/generate-square.js`** — proven write recipe (clone known-good template →
  mutate geometry + regenerate UUIDs).

## Cheat sheet

- Target `version: "3.0.4"` (what current MakeIt! writes; what we verified opens).
  Objects carry Fabric `"version":"5.3.0"`.
- Units are **millimetres**. Effective size = `width*scaleX` × `height*scaleY`.
- **`#E61F19` (red) stroke = CUT.** `processMode` ∈ `cut|engrave|fillEngrave`.
- Objects join to `processList` and `layerDataList` by object `id` (`"el-<uuid>"`).
- Real geometry = Fabric `path` arrays: `[["M",x,y],["L",x,y],["C",...],...]`,
  absolute coords, only `M/L/C/Q/Z` (pre-simplify SVG: expand `H/V/S/T/A` + relatives).
- Lowest-risk generation: **clone a same-version known-good template**, don't build
  JSON from scratch. Regenerate `projectId`, canvas `id`, object `id`(s); re-key
  `processList`; re-point `layerDataList`; set `name`, `time=Date.now()`,
  `currentCanvasId`.
- **Always have the human open generated files in MakeIt! to verify** dimensions and
  operation — there's no public validator, and field sets drift by version.

## Sample file locations (not in repo)

The user's full `.wws` library lives at `~/Desktop/cups/WWS/` (hundreds of files,
2.0.x–3.0.x). Good structural references there: `6mm-box.wws` (paths at scale 1, mm
coords) and `6MM-5X5-DICE-HOLDER.wws` (paths in large coord space + ~0.0257 scale).

## SVG → `.wws` converter (built — `svg2wws`)

Go single-binary converter lives in `cmd/svg2wws` (CLI) + `internal/conv` (logic);
spec is **`docs/svg2wws.md`** (and `docs/svg2wws-agent.md` for invoking it from
another repo). Build: `go build -o svg2wws ./cmd/svg2wws`. Run:
`./svg2wws --in design.svg --material 300x200`. It parses SVG (transforms + units →
absolute mm, all commands simplified to `M/L/C/Q/Z`), groups subpaths into pieces by
even/odd containment (outer + holes), **raster-nests** the true polygon footprints
onto sheets (free rotation, ~3mm spacing, holes are free space, multi-sheet spill →
one canvas per sheet), and emits the v3.0.4 envelope (coords baked absolute, scale 1,
`left/top` = exact path bbox min). Tests: `go test ./...` (covers path simplify, arc,
hole grouping, unit scale, no-overlap/spacing). Defaults: `--margin 5 --spacing 3
--grid 1.0 --rotations 8`; oversized single pieces error out.

**Not yet hardware-verified** — outputs pass structural + no-overlap validation but no
converter file has been confirmed to open in MakeIt!. Have the user test one.

### Next directions (not yet built)

- Verify converter output opens in MakeIt!; confirm the exact SVG-unit→mm import rule.
- Engrave / fill-engrave layer mapping (multiple stroke colors → processModes), kerf
  compensation, `<use>`/`<text>`/`<image>` support, smarter (GA) nesting.
- `~/repos/design-explorer` (indexes the user's purchased laser SVG/6 mm design packs)
  is a natural SVG source and a possible host for a "Convert to .wws" feature.
